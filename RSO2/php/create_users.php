
<?php
/*
    require_once('db.php');

    $sql="create table clani_drustvo (
        id INT auto_increment primary key,
        first_name VARCHAR(50),
        last_name VARCHAR(50),
        password VARCHAR(50),
        email VARCHAR(50)
    );";

    if($db->query($sql))
        echo("Tabela RSO_users je bila uspesno ustvarjena.");
    else
        echo("Napaka pri ustvarjanju tabele.");
    */
?>
